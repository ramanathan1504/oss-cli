# Filtered by Maven at build time -- see the <resources> block in pom.xml.
# Do not hand-edit: the literal ${...} below are placeholders, not values.
version=${project.version}
buildTime=${maven.build.timestamp}
